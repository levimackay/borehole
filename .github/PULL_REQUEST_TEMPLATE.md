## What does this change, and why?

<!-- Describe the change and the reasoning behind it. Link any relevant issue. -->

## Checklist

- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo test --workspace` passes
- [ ] `npm run build` passes (if frontend/`src-tauri` code changed)
- [ ] Tests added or updated for the change (if applicable)
- [ ] Docs updated (`docs/ARCHITECTURE.md`, `README`, etc.) if behavior or structure changed
