# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
/target
# Cargo.lock is committed: this workspace builds binaries (cli, desktop app),
# not a published library, so a checked-in lockfile is the correct practice.

# Borehole's own index/cache data (never commit indexed repo data)
.borehole/
*.borehole.db

# Tauri build output
src-tauri/target
src-tauri/gen

# Coverage
coverage/
*.profraw

# fixture-generated git dirs are checked in deliberately via build-fixtures.sh, not gitignored
